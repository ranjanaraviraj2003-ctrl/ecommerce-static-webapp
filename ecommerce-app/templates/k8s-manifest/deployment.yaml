apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecommerce-deployment
  namespace: ranjana-ns

spec:
  replicas: 3
  selector:
    matchLabels:
      app: ecommerce
  template:
    metadata:
      labels:
        app: ecommerce
    spec:
      containers: 
      - name: vegitable-app
        image: ranjana10703/e-commerce-app:v3.0.0
        ports:
        - containerPort: 80