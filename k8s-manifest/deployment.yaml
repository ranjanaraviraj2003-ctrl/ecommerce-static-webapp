apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecommerce-deployment
  namespace: ecommerce-ns

spec:
  replicas: 3
  selector:
    matchLabels:
      app: ecommerce
  template:
    metadata:
      labels:
        app: ecommerce
    spec:
      containers: 
      - name: vegitable-app
        image: ranjanan10703/e-commerce-app:v1.0.1
        ports:
        - containerPort: 80
